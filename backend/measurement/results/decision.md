# NeuLitTrace v2 -- Measurement Gate Decision

Generated by `python -m backend.measurement.run_gate`. Sample size and units
are explicit per section so every number here is directly quotable.

## 1. Retrieval parity -- FakeRetrieval baseline vs Cortex Search (live, pending)

**Claim to defend:** moving retrieval from v1's local BM25+MiniLM hybrid to
Snowflake Cortex Search + rarity re-rank did not cost retrieval quality.

**What's computed here:** recall@10 for `backend.contracts.fakes.FakeRetrieval`
(deterministic token-overlap + rarity scoring over `backend/data/corpus.json`,
n=28 gold-set queries) -- this stands in for "the
retrieval-quality floor a local lexical scorer with the same rarity formula
achieves," since it is architecturally comparable to v1's BM25+MiniLM
baseline and needs no credentials.

**What's NOT computed here:** `CortexSearchRetriever`'s live recall@10.
That requires a real Cortex Search Service serving `NEULIT.CORE.PAPERS` --
unavailable in this sandbox (no Snowflake credentials). A human with
credentials must re-run this script under `NEULIT_PROFILE=live_no_memory`
once the search service is `ACTIVE` (see `snowflake/sql/README.md`), and
report the `CortexSearchRetriever` recall@10 alongside the numbers below to
actually close this claim.

| Overall recall@10 (FakeRetrieval baseline, n=28) | Rare-condition recall@10 (n=20) |
|---|---|
| 0.60 | 0.67 |

Per-query detail:

| Query | Condition | Rare | recall@10 |
|---|---|---|---|
| scalp angiosarcoma FDG-PET case report | Scalp angiosarcoma | yes | 0.67 |
| semantic variant primary progressive aphasia imaging | Primary progressive aphasia semantic variant | yes | 0.70 |
| corticobasal syndrome frontoparietal cortex PET | Corticobasal syndrome | yes | 0.60 |
| midbrain hypometabolism in progressive supranuclear palsy | Progressive supranuclear palsy | yes | 0.60 |
| frontotemporal dementia anterior temporal involvement | Frontotemporal dementia | yes | 0.60 |
| Creutzfeldt-Jakob disease parietal occipital PET | Creutzfeldt-Jakob disease | yes | 0.70 |
| dementia with Lewy bodies occipital hypometabolism | Dementia with Lewy bodies | yes | 0.80 |
| anti-NMDA receptor encephalitis basal ganglia involvement | Anti-NMDA receptor encephalitis | yes | 1.00 |
| primary angiitis of CNS MCA territory findings | Primary angiitis of CNS | yes | 0.80 |
| neurolymphomatosis leptomeningeal PET | Neurolymphomatosis CNS involvement | yes | 0.00 |
| Alzheimer's disease FDG-PET temporal parietal pattern | Alzheimer's disease probable typical FDG-PET pattern | no | 0.00 |
| temporal lobe epilepsy interictal FDG-PET | Temporal lobe epilepsy interictal FDG-PET | no | 0.70 |
| acute ischemic stroke MCA territory PET | Acute ischemic stroke MCA territory acute phase | no | 0.60 |
| Parkinson's disease basal ganglia PET | Parkinson's disease motor form later stage | no | 0.50 |
| cutaneous angiosarcoma helical tomotherapy scalp brachytherapy | Scalp angiosarcoma | yes | 1.00 |
| Huntington disease presenting as primary progressive aphasia semantics | Primary progressive aphasia semantic variant | yes | 0.30 |
| flortaucipir PET MRI 4R tauopathy corticobasal degeneration | Corticobasal syndrome | yes | 0.40 |
| AV1451 tau retention progressive supranuclear palsy eye movement disorder | Progressive supranuclear palsy | yes | 0.50 |
| MAPT mutation behavioral variant frontotemporal dementia biomarker | Frontotemporal dementia | yes | 0.70 |
| DPA-714 PET MRI neuroinflammation sporadic Creutzfeldt-Jakob disease | Creutzfeldt-Jakob disease | yes | 0.90 |
| amyloid positivity probable dementia with Lewy bodies REM sleep behavior disorder | Dementia with Lewy bodies | yes | 0.90 |
| NMDAR GFAP mGluR5 tripartite autoantibody autoimmune encephalitis | Anti-NMDA receptor encephalitis | yes | 0.90 |
| granulomatous primary angiitis central nervous system brain PET MRI | Primary angiitis of CNS | yes | 0.60 |
| diffuse large B-cell lymphoma central peripheral nervous system involvement | Neurolymphomatosis CNS involvement | yes | 0.75 |
| amyloid PET typical Alzheimer's disease biomarker pattern | Alzheimer's disease probable typical FDG-PET pattern | no | 0.00 |
| interictal FDG-PET hypometabolism seizure focus localization epilepsy | Temporal lobe epilepsy interictal FDG-PET | no | 0.70 |
| middle cerebral artery territory infarct acute stroke perfusion imaging | Acute ischemic stroke MCA territory acute phase | no | 0.80 |
| nigrostriatal dopaminergic degeneration motor Parkinson's disease imaging | Parkinson's disease motor form later stage | no | 0.20 |

## 2. Cost per query

**Not computed.** snowflake unavailable in this environment. This requires a live Snowflake account with SNOWFLAKE_* credentials set and at least one real /query run recorded in NEULIT.CORE.TOKEN_LEDGER. A human with credentials must run:

```bash
export NEULIT_PROFILE=live_no_memory
python -m backend.measurement.run_gate
```

after running a handful of real /query requests, then re-check this file.

## 3. Latency per call site

See the `latency_by_call_site` table in section 2 above (`V_COST_BY_CALL_SITE.AVG_LATENCY_MS`)
-- median/p95 latency *per call*, not per call site, would require raw
per-event `LATENCY_MS` rows from `TOKEN_LEDGER` rather than the pre-aggregated
view; also not computable without a live account with real traffic. Median/p95
require a human with credentials to compute from
`SELECT LATENCY_MS FROM NEULIT.CORE.TOKEN_LEDGER WHERE CALL_SITE = '<site>'`
per call site once real traffic exists.

## 4. Cost of Intelligence -- compression + caching (Phase C, added post-CP1)

Generated by `python -m backend.measurement.run_cost_of_intelligence`, run
against the same 28-query gold set as section 1, with zero Snowflake
credentials -- retrieval, compression scoring, and cache key/TTL logic are
all real, unmocked code (`backend/app/llm/compress.py`,
`backend/app/llm/cache.py`, `backend/snowflake/llm.py`'s
`CortexLLMClient.chat()`); only the Cortex COMPLETE transport call
(`CortexLLMClient._call_complete`) is stubbed to a deterministic response,
since no live account is available in this sandbox. Every number below is
copy-pasted from the script's actual stdout, not estimated.

Exact command and full output:

```
$ python -m backend.measurement.run_cost_of_intelligence
=== Compression ===
n_queries=28 n_abstracts=280
tokens_before_total=64947 tokens_after_total=42401
overall_reduction_pct=34.71%

=== Cache ===
hits=28 misses=28 hit_rate=0.5
estimated_cost_saved_usd=0.032256
ledger_events_written=56

=== Cost (compression-only, representative summary call) ===
baseline_cost_usd=0.009
reduced_cost_usd=0.00775044
estimated_cost_reduction_pct=13.88%
```

**Token-reduction %:** 34.71% -- weighted across all 280 paper abstracts
(28 gold-set queries x top-10 retrieved papers each) compressed from
top-N=4 extractive sentence selection, 64,947 estimated tokens before ->
42,401 after (`backend/app/llm/tokenizer.py`'s `estimate_tokens`).

**Cache hit-rate %:** 50% -- each of the 28 gold-set queries' `hyde` call
was issued twice through the real `CortexLLMClient.chat()` -> `TTLPromptCache`
path (simulating a same-session repeat, e.g. pagination or a retry): 28
misses (first issue) + 28 hits (repeat issue) = 56 calls, 28/56 = 0.5. This
is a synthetic *2-calls-per-query* traffic pattern chosen to exercise the
real cache mechanics end-to-end without a live account, not a claim about
production hit-rate under organic multi-user traffic -- a human with
credentials should re-run this with `NEULIT_PROFILE=live_no_memory` against
real session traffic (see section 2) to get an organic number.

**Estimated cost-reduction %:** 13.88% -- compression-only, computed on a
representative 1000-prompt-token/300-completion-token `summary`-shaped call
using the real `compute_cost_usd()` function against the same published
`claude-3-5-sonnet` pricing row researched for `Handoff-Log.md` item 4
(1.8 credits/Mtok in, 9.0 credits/Mtok out, $2/credit): baseline
$0.009000 -> $0.007750. This number is compression's contribution only, on
one representative call shape; it is additive with (but not summed into)
the separate `hyde` cache savings reported above
(`estimated_cost_saved_usd=$0.032256` for this synthetic run), since caching
and compression act on different call sites (`hyde`/`relevance_check` vs
`summary`/`citation_check`).

**What this does NOT show:** a live Snowflake account's real credit
consumption, or an organic (non-synthetic) cache hit-rate from actual
multi-user /query traffic. Both require the same live-account re-run
already called for in section 2 above.

Raw JSON: `backend/measurement/results/cost_of_intelligence.json`.
